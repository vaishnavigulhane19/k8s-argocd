apiVersion: v1
kind: Service
metadata: 
  name: my-svc
  labels: 
    app: myapp
spec: 
  type: ClusterIP
  selector: 
    app: myapp
  ports:
    - name: my-svc-port
      protocol: TCP
      port: 80
      targetPort: 80